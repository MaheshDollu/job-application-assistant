spring:
  application:
    name: job-application-assistant
  ai:
    ollama:
      base-url: http://localhost:11434
      chat:
        options:
          model: llama3
          temperature: 0.7

server:
  port: 8080

logging:
  level:
    com.mahesh: DEBUG
    org.bsc.langgraph4j: INFO
